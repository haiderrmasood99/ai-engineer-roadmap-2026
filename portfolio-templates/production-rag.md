# Portfolio Template: Production RAG

Build RAG with ingestion, hybrid retrieval, query expansion, reranking, evaluation, and CI gates.

Minimum features: chunking, retrieval, eval dataset, threshold file, GitHub Actions.

Proof of production thinking: document retrieval failure modes and quality regression checks.
